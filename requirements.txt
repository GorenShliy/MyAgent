# ================= MyAgent 项目依赖 =================
# LLM + Embedding（OpenAI 兼容接口，同时用于对话与向量化）
openai>=1.40.0

# 本地向量库（持久化，无需额外安装数据库服务）
chromadb>=0.5.0

# PDF 文档解析
pypdf>=4.0.0

# 文本递归切分（轻量独立包，仅使用其中的 splitter）
langchain-text-splitters>=0.3.0

# MCP 协议 SDK（客户端 + 内置 Server 均使用，要求 Python 3.10+）
# 注意：锁定 <2 —— mcp 2.x 已将 FastMCP 改名为 MCPServer 且 API 大改，本项目按 1.x 编写
mcp>=1.2.0,<2

# HTTP 工具底层请求库
httpx>=0.27.0

# Web 界面（streamlit run web/app.py）
streamlit>=1.37.0

# ---- 以下为可选依赖，按需启用 ----
# sentence-transformers>=3.0.0   # 本地模型重排（开启 Rerank 时安装）
# tiktoken>=0.7.0                # 将文本切分改为按 token 切分时安装（见 rag/splitter.py 注释）